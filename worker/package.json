{
  "name": "status-page-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate": "wrangler d1 migrations apply status-page-db --remote",
    "migrate:local": "wrangler d1 migrations apply status-page-db --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
