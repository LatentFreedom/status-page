# Optional build-time override for the feed base URL. When unset, the
# frontend uses `apiBase` from status.config.ts at the repo root.
# Useful for local development against `wrangler dev`:
# NEXT_PUBLIC_UPTIME_API=http://localhost:8787/api/v1
